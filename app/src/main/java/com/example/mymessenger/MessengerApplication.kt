package com.example.mymessenger

import android.app.Application

class MessengerApplication : Application() {

    override fun onCreate() {
        super.onCreate()
        // Здесь можно инициализировать любые библиотеки
    }
}